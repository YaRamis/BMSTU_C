#include "matrix_operations.h"

int find_min_null_matrix(int **matrix, int rows, int columns, int **null_matr, int *rows_null_matr, int *columns_null_matr)
{
    int *ptr_b, *ptr_e;
    for (int i = 0; i < rows; i++)
    {
        for (int j = 0; j < columns; j++)
        {
            if (matrix[i][j] = 0)
                ptr_b = matrix[i] + sizeof(int)
        }
    }
}